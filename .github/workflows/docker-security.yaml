name: Docker Security
on:
  workflow_call:
    inputs:
      PROJECT:
        type: string
        required: true

jobs:
  security:
    runs-on: ubuntu-22.04
    steps:
      - name: Download a Build Artifact
        uses: actions/download-artifact@v3.0.0
        id: download
        with:
          name: artifacts_image_${{github.sha}}
      - name: docker load
        run: docker image load --input image_${{inputs.PROJECT}}_${{github.sha}}.tar
      - name: scan
        uses: anchore/scan-action@v3
        continue-on-error: true
        with:
          image: image_${{inputs.PROJECT}}:${{github.sha}}
          output-format: table
